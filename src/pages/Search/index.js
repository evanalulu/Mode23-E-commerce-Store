import React from 'react';
import ProductRes from '../../components/ProductRes';

const Search = ({ }) => {
    return (
        <div className='searchPage'>
            <ProductRes />
        </div>
    );
};

export default Search;
